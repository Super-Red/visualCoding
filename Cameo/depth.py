import numpy

# Devices.CAP_OPENNI = 900 # OpenNI (for Microsoft) CAP_OPENNI_ASUS = 910 # OpenUI (for Asus Xtion)
# Channels of an OpenNI_compatible depth generator.CAP_OPENNI_DEPTH_MAP = 0 # Depth values in mm(16UC1)CAP_OPENNI_POINT_CLOUD_MAP = 1 #XYZ in meters(32FC3)CAP_OPENNI_DISPARITY_MAP = 2 #Disparity in pixels (8UC1)CAP_OPENNI_DISPARITY_MAP_32F = 3 # Disparity in pixels(32FC1)CAP_OPENNI_VALID_DEPTH_MASK = 4 #8UC1
# Channels of an OpenNI_compatible RGB image generator.CAP_OPENNI_BGR_IMAGE = 5CAP_OPENNI_GRAY_IMAGE = 6

